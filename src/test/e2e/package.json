{
  "devDependencies": {
    "cypress": "^14.2.0",
    "cypress-terminal-report": "^7.1.0",
    "eslint-plugin-cypress": "^4.2.1"
  },
  "scripts": {
    "cy": "npx cypress run",
    "lint": "npx eslint cypress/tests"
  }
}
